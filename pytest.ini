[pytest]
testpaths = kindle_math_converter/tests
markers =
    slow: exercises real external resources (a real book from the user's library, or a cached MinerU parse) — not hermetic, so keep it opt-out rather than opt-in. Run `pytest -m "not slow"` for the fast subset (milliseconds); plain `pytest` runs everything including these.
